<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mychef">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCHEF"
        tools:targetApi="31">

        <!-- Giriş ekranı (Artık burası LAUNCHER) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Kayıt ekranı -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true" />

        <!-- Rol seçimi ekranı -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- Tarif yolcusu paneli -->
        <activity android:name=".YolcuPanelActivity" android:exported="true" />

        <!-- Şef paneli -->
        <activity android:name=".ChefPanelActivity" android:exported="true" />

        <!-- Malzeme ekranı -->
        <activity android:name=".MalzemeActivity" android:exported="false" />

        <!-- Tools ekranı -->
        <activity android:name=".ToolsActivity" android:exported="true" />

        <!-- Profile ekranı -->
        <activity android:name=".ProfileActivity" />

        <!-- EditProfile ekranı -->
        <activity android:name=".EditProfileActivity" />

        <!-- BecomeChef ekranı -->
        <activity android:name=".BecomeChefActivity" />

        <!-- ChefDetail ekranı -->
        <activity android:name=".ChefDetailActivity" />

        <!-- Bildirim servisi -->
        <service
            android:name=".FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
